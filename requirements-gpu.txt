fastapi
uvicorn[standard]
websockets
python-multipart
numpy
soundfile
faster-whisper
pyannote.audio>=3.3,<4.0
torch==2.4.1
torchaudio==2.4.1
transformers>=4.40
huggingface_hub>=0.24,<1.0
# CrisperWhisper 2.0 (STT_ENGINE=crisperwhisper) — backend CTranslate2, le
# même runtime que faster-whisper. La conversion CT2 du modèle, faite une
# seule fois au premier chargement, a besoin de torch + transformers ; le
# plafond transformers<4.45 a donc été levé (pyannote ne l'épingle pas).
# ⚠️ Poids sous licence NON COMMERCIALE (Nyra Health) — cf. README.
# ⚠️ CrisperWhisper exige le fork « ctranslate2-crisperwhisper », qui fournit
# le même module que le CTranslate2 amont de faster-whisper : le Dockerfile
# le réinstalle EN DERNIER (l'ordre de pip ne suffit pas). Hors Docker,
# refaire à la main : pip uninstall ctranslate2 && pip install ctranslate2-crisperwhisper
crisperwhisper[ct2]
